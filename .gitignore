# Rust build artifacts
/target/
# Servers workspace build artifacts (separate workspace, separate target dir)
/servers/target/
# arm64 smoltcp compile gate is its own detached workspace with its own target dir
/servers/smoltcp-gate/target/
# ELF loader smoke-test binary — detached workspace, own target dir
/servers/elf-smoke/target/
# Linux-personality smoke-test binary — detached workspace, own target dir
/servers/linux-smoke/target/
# Linux FS-syscall smoke-test binary — detached workspace, own target dir
/servers/fs-smoke/target/
# Linux threads/futex smoke-test binary — detached workspace, own target dir
/servers/threads-smoke/target/
# Container-isolation smoke-test binary — detached workspace, own target dir
/servers/isolation-smoke/target/
# Container rootfs-confinement smoke-test binary — detached workspace, own target dir
/servers/confine-smoke/target/

# Editor files
*.swp
*.swo
*~
.vscode/
.idea/

# OS files
.DS_Store
Thumbs.db

# mdbook output
/docs/book/

# QEMU state
*.qcow2
*.img
